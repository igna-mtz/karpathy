[project]
name = "karpathy"
version = "0.1.0"
description = "Exercises from Andrej Karpathy's micrograd course"
requires-python = ">=3.12"
dependencies = [
    "ipython>=9.0",
    "jupyterlab>=4.0",
    "torch>=2.0",
]
